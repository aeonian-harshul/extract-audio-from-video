# extract-audio-from-video
Multi-Threaded Audio Extractor (-c:a copy) 
A high-performance Windows Batch script that recursively scans nested folders to rip audio tracks directly from MP4 videos without re-encoding. It uses ffprobe to automatically detect codecs (AAC, MP3, etc.) and assigns the proper file extensions dynamically , processing 16 files simultaneously to maximize hardware utilization
